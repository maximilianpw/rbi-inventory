PORT=3001
CLERK_SECRET_KEY=your_clerk_secret_key_here

# DATABASE_URL is automatically set by devenv via:
# postgresql://postgres@/rbi_inventory?host=$DEVENV_STATE/postgres&sslmode=disable
# Only set this manually if running outside of devenv
DATABASE_URL=
